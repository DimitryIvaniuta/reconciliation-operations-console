# GitHub repository metadata

**Repository name:** `reconciliation-operations-console`

**Description:** React 19 operations console for Kafka/PostgreSQL reconciliation, immutable data-quality reporting, and resumable replay management.

**Suggested topics:**

`react` `typescript` `vite` `kafka` `postgresql` `data-quality` `reconciliation` `playwright` `tanstack-query` `devops-dashboard`

After authenticating GitHub CLI:

```bash
gh repo create reconciliation-operations-console \
  --source=. \
  --public \
  --description "React 19 operations console for Kafka/PostgreSQL reconciliation, immutable data-quality reporting, and resumable replay management." \
  --push

gh repo edit --add-topic react,typescript,vite,kafka,postgresql,data-quality,reconciliation,playwright,tanstack-query,devops-dashboard
```

Repository creation is intentionally not performed automatically because this build environment has no authorized GitHub account.
